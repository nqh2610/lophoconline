# Database Connection
# Thay đổi username, password và database_name theo cấu hình PostgreSQL của bạn
DATABASE_URL=postgresql://username:password@localhost:5432/lophoc_online

# PostgreSQL Configuration
PGHOST=localhost
PGPORT=5432
PGUSER=postgres
PGPASSWORD=your_postgres_password_here
PGDATABASE=lophoc_online

# Session Secret (QUAN TRỌNG: Thay đổi thành chuỗi ngẫu nhiên ít nhất 32 ký tự)
SESSION_SECRET=change-this-to-a-random-secret-key-at-least-32-characters-long

# Node Environment
NODE_ENV=development

# ============================================
# HƯỚNG DẪN SỬ DỤNG:
# ============================================
# 1. Copy file này thành .env
# 2. Thay đổi các giá trị sau:
#    - username → tên user PostgreSQL của bạn (mặc định: postgres)
#    - password → mật khẩu PostgreSQL của bạn
#    - your_postgres_password_here → mật khẩu PostgreSQL
#    - SESSION_SECRET → tạo chuỗi ngẫu nhiên (có thể dùng: https://randomkeygen.com/)
# 3. Đảm bảo database 'lophoc_online' đã được tạo trong PostgreSQL
# 4. Chạy: npm run db:push để tạo bảng
# 5. Chạy: npm run dev để khởi động ứng dụng
