# ==========================================
# MYSQL DATABASE CONFIGURATION
# ==========================================
# File này là template cho MySQL connection
# ==========================================

# MySQL Database Connection
# Format: mysql://username:password@host:port/database
DATABASE_URL=mysql://root:your_password@localhost:3306/lophoc_online

# Session Secret (QUAN TRỌNG: Thay đổi thành chuỗi ngẫu nhiên ít nhất 32 ký tự)
SESSION_SECRET=change-this-to-a-random-secret-key-at-least-32-characters-long

# Node Environment
NODE_ENV=development

# ============================================
# HƯỚNG DẪN SỬ DỤNG:
# ============================================
# 1. Copy file này thành .env:
#    cp .env.mysql.example .env
#
# 2. Thay đổi các giá trị:
#    - root → tên user MySQL của bạn (thường là 'root')
#    - your_password → mật khẩu MySQL của bạn
#    - localhost → địa chỉ MySQL server (local thường là localhost)
#    - 3306 → port MySQL (mặc định là 3306)
#    - lophoc_online → tên database
#    - SESSION_SECRET → chuỗi ngẫu nhiên
#
# 3. Ví dụ cụ thể:
#    DATABASE_URL=mysql://root:mypass123@localhost:3306/lophoc_online
#    SESSION_SECRET=abc123xyz789randomsecret12345678901234567890
#
# 4. Tạo database trong MySQL:
#    mysql -u root -p
#    CREATE DATABASE lophoc_online CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
#
# 5. Chạy migration:
#    npm run db:push
#
# 6. Khởi động ứng dụng:
#    npm run dev
#
# ============================================
# LƯU Ý BẢO MẬT:
# ============================================
# - KHÔNG commit file .env lên Git
# - Sử dụng mật khẩu mạnh cho MySQL
# - Thay đổi SESSION_SECRET thành chuỗi ngẫu nhiên
# - File .gitignore đã được cấu hình để bỏ qua .env
